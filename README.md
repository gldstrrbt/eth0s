# stranger-friends
Collecting the mac addresses of wlan interfaces via a monitor mode enabled wireless adapter connected to a Raspberry Pi. Those MAC addresses are then pushed into a JSON file. From there the JSON file is parsed and the MAC addresses are converted from hexadecimal values into values that can be used for attribute values within Cinema 4D and Blender. 
